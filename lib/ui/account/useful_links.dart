import 'package:flutter/material.dart';

class UsefulLinksPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Parametres de notificatiion'),
      ),
      body: Center(
          //child: Text('Contenu des conditions générales'),
          ),
    );
  }
}
