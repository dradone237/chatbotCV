import 'package:flutter/material.dart';

class PreferencesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('PREFERENCE'),
      ),
      body: Center(
          //child: Text('Contenu des conditions générales'),
          ),
    );
  }
}
